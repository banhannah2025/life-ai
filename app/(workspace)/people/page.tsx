import { SearchView } from "@/components/search/SearchView";

export default function PeopleDirectoryPage() {
  return <SearchView mode="profiles" heading="People directory" placeholder="Search by name, company, or skill…" />;
}
